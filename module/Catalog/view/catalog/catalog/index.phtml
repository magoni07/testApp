<?php
print $this->showMessages();
?>

<ul>
    <?php

    foreach($this->products as $product) {
        print '<li>
            <a href="' . $this->url('catalog', array('action'=>'view', 'id' => $product->getId())) . '">' . $product->getName() . '</a>
            </li>';
    }
    ?>
</ul>